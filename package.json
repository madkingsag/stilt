{
  "devDependencies": {
    "@foobarhq/eslint-config": "^4.0.1",
    "babel-cli": "^6.26.0",
    "babel-plugin-transform-class-properties": "^6.24.1",
    "babel-plugin-transform-decorators-legacy": "^1.3.4",
    "babel-preset-env": "^1.6.1",
    "babel-preset-flow": "^6.23.0",
    "eslint": "^4.19.1",
    "flow-bin": "^0.71.0",
    "lerna": "^2.11.0"
  },
  "scripts": {
    "build": "NODE_ENV=production lerna exec --parallel -- babel src -d dist --copy-files",
    "build:watch": "NODE_ENV=production lerna exec --parallel -- babel src -d dist --copy-files --watch",
    "fix-npm": "lerna exec -- rm -rf node_modules && rm package-lock.json && lerna bootstrap",
    "publish": "npm run build && lerna publish --access public"
  },
  "dependencies": {}
}
